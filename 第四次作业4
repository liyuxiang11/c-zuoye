#include<iostream>
using namespace std;
int main(){
    int n;
    cin>>n;
    if(n<60) {cout<<"E";}
    else if(60<=n&&n<70) {cout<<"D";}
    else if(70<=n&&n<80) {cout<<"C";}
    else if(80<=n&&n<90) {cout<<"B";}
    else if(n>=90) {cout<<"A";}
    return 0;
}
